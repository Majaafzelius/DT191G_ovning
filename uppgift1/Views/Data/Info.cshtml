@{
    ViewData["Title"] = "Info Page";
}

<h2>Display Data</h2>

@if (ViewBag.FormData != null)
{
    <p>Name: @ViewBag.FormData.Name</p>
    <p>Email: @ViewBag.FormData.Email</p>
}
else
{
    <p>No data available.</p>
}
